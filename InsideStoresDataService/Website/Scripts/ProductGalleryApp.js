
(function () {
    "use strict";

    angular.module("myApp", ["ngAnimate"]);

    var app = angular.module("myApp");

    app.run(["$rootScope", function ($rootScope) {
        
        // anything needed to run after module initialized.

    }]);

}());
